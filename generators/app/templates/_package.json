
{
  "name": "hexo-plugin-<%= pluginName %>",
  "description": "<%= pluginDesc %>",
  "version": "0.0.1",
  "main": "index",
  "scripts": {
  	"postversion": "git push && git push --tags && git push"
  },
  "dependencies":  {
  }
}